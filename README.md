# WeatherAPI-Srcapper-DB-loader
A lightweight weather data pipeline that fetches historical data via API for a user-defined location and date range. It transforms and stores data in CSV, with optional SQL Server upload. Processed files move from docs to archive, following a clean ETL design.
